import {describe,expect,it} from 'vitest'; import {parseMetroResponse} from './metroParser'; import type {MetroStation} from '../../types/transport';
const station:MetroStation={id:'x',name:'X',apiCode:'x',lines:['1']};
describe('parseMetroResponse',()=>{it('normalizes numeric strings and ages predictions',()=>{const now=Date.parse('2026-01-01T10:05:00Z'); const r=parseMetroResponse([{linea:'1',sentido:'Pinar',anden:'2',proximo:'7',siguiente:'12',fechaHoraEmisionPrevision:'2026-01-01T10:03:00Z'}],station,now); expect(r.arrivals.map(x=>x.minutes)).toEqual([5,10]);}); it('ignores malformed rows and negative arrivals',()=>{const r=parseMetroResponse([{linea:'',proximo:2},{linea:'1',sentido:'A',proximo:'bad',siguiente:-1}],station); expect(r.arrivals).toHaveLength(0);}); it('deduplicates equivalent arrivals',()=>{const p=[{linea:'1',sentido:'A',proximo:3,siguiente:8},{linea:'1',sentido:'A',proximo:3,siguiente:8}]; expect(parseMetroResponse(p,station).arrivals).toHaveLength(2);}); it('does not trust invalid timestamps as valid freshness',()=>{const r=parseMetroResponse([{linea:'1',sentido:'A',proximo:3,fechaHoraEmisionPrevision:'invalid'}],station); expect(r.sourceUpdatedAt).toBeUndefined();});});
